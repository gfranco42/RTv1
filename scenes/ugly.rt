camera

eye = focal point = position?			E

vectors:
-	U									right
-	V									vertical
-	W									forward
-	UP									UP
= Orientation

Look at point = ( 0, 0, 0 );			L

forward =	normalize(E - L);
			normalize(vec_sub(E, L));

right = 	normlize(cross(UP, forward));

vertical =	cross(forward, right);

cam:

position
target
up
